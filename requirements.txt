# --- Core App Framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.1

# --- Model Inference & Transformers ---
transformers>=4.39.0
sentence-transformers>=2.3.0
evaluate>=0.4.1

# --- FAISS for Vector Search ---
faiss-cpu>=1.7.4
# faiss-gpu>=1.7.4  # Uncomment this if you're using GPU FAISS

# --- PyTorch for GPU inference (match with your CUDA version) ---
torch==2.1.2+cu121
--extra-index-url https://download.pytorch.org/whl/cu121

# --- PDF, Word, Image Parsing for RAG Materials ---
PyMuPDF>=1.23.0        # For extracting from PDFs
python-docx>=1.1.0      # For extracting from DOCX
pytesseract>=0.3.10     # For OCR
pillow>=10.0.0          # For image processing

# --- NLP Utilities ---
nltk>=3.8.1
scikit-learn>=1.3.0     # For cosine similarity, evaluation
numpy>=1.25.0
pandas>=2.0.0

# --- Optional Logging / Analytics Tools ---
loguru>=0.7.0           # Pretty logging (optional)
rich>=13.0.0            # Pretty console output (optional)

# --- CORS (already included via uvicorn[standard], listed for clarity) ---
starlette>=0.37.2

# --- For Future DevOps/API Extensions ---
httpx>=0.25.0           # For async HTTP client
